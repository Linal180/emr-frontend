// packages block
import { FC } from 'react';
// component block
import AppointmentForm from "../appointmentForm";

const AddAppointmentComponent: FC = () => <AppointmentForm />;

export default AddAppointmentComponent;
