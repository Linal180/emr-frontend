// packages block
import { FC } from 'react';
// component block
import ServiceForm from '../serviceForm';

const AddFacilityServiceComponent: FC = (): JSX.Element => <ServiceForm />;

export default AddFacilityServiceComponent;
