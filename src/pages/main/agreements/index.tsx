// packages block
import { FC } from "react";
// components block
import AgreementComponent from "../../../components/main/agreements";

export const Agreements: FC = (): JSX.Element => <AgreementComponent />;
