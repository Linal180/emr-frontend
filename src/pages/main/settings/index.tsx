// packages block
import { FC } from "react";
// components block
import { SettingsComponent } from "../../../components/main/settings";

export const Settings: FC = () => <SettingsComponent />;

export default Settings;
