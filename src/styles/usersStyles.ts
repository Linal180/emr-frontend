import { makeStyles } from "@material-ui/core";

export const useUsersStyles = makeStyles((theme) => ({
  dialogPaper: {
    overflow: 'visible'
  },

  iconButton: {
    padding: 8
  },

  helperText: {
    color: "red"
  }
}));

