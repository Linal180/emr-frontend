query FindAllReactions($reactionInput: ReactionInput!){
  findAllReactions(reactionInput: $reactionInput){
    reactions {
      id
      name
    }

    pagination{
      page
      limit
      totalCount
      totalPages
    }
  }
}
